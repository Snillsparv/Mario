// PLACEHOLDER — replaced by the synthesized SFX + original music engine.
export class AudioEngine {
  constructor(events) {
    this.events = events;
  }
  unlock() {}
  play(name, opts) {}
  playMusic(name) {}
  stopMusic() {}
  setListener(pos, yaw) {}
  update(dt) {}
}
